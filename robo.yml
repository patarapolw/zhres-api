build:
  command: |
    docker-compose build
start:
  command: |
    export PORT=8080
    echo "Go to http://localhost:$PORT"
    docker-compose up
api:
  command: |
    export PORT=26670
    echo "Go to http://localhost:$PORT"
    docker-compose up
